# 吸收报告：dsh-feishu-bridge-plugin 0.3.0（/mode + 正式测试，吸星大法收尾轮）

> 目标：补吸最后一个有意丢弃项——/mode 多模式 Agent 切换；并按用户要求为全部新功能建立正式单元测试。
> 参考基准不变：[amlyczz/dsh-lark-link](https://github.com/amlyczz/dsh-lark-link)（⭐11，活跃，MIT）。

## 可吸收点清单（本轮）

| # | 吸收点 | 来源 | 为什么吸 | 吸收方式 | 落地情况 |
| --- | --- | --- | --- | --- | --- |
| P2.5-1 | /mode 单选卡命令（实时 roster 枚举 + 点击即切） | lark-link src/index.ts:1029-1074 + modeCard | 用户拍板补吸（此前因「依赖 DSH preset 快照重建」评级低） | 借鉴改造 | ✅ commands.ts（3 commit：bb745b7/d616e35/a1368f2） |
| P2.5-2 | rotate() 会话重建语义（新行、旧行保留、无 id 冲突） | lark-link rotate/runNonce | 切换 preset 必须重建（preset 在 agent 创建时快照） | 化用：epoch+1 复用 /reset，**零新机制** | ✅ resetChatSession 共享（commands.ts:214-230） |
| 测试化 | 正式单元测试基建（lark-link 有 156 测试，本机零测试短板） | lark-link test/ 组织方式（vitest） | 用户明确要求：功能开发必须配测试 | 借鉴组织 + 全模块覆盖 | ✅ test/ 13 文件 193 用例全绿 |

## 关键化用差异

| 点 | lark-link | 本机 0.3.0 |
| --- | --- | --- |
| 会话重建 | rotate() mint 新 runNonce（新机制） | 复用现有 epoch+1（会话 id 本就 `feishu-<epoch>-<slug>` 派生），提取 resetChatSession 共享 |
| 预设显示名 | label 字段 | 宿主实测 API 是 `name ?? id`（初版镜像 label 报 TS2353，已修） |
| 内置预设表 | AGENT_PRESETS 四预设（PTC/标准/极简/创造） | 不内置——本机宿主 agentPresets 服务实时枚举，缺失时只承诺 standard（化用为 liveModeRoster + 降级） |
| 测试 | 156 用例随源码 | 193 用例覆盖本机全部 13 模块 + 可测面分析 |

## 落地对照与验证

**Git 提交（main）**：
```
9ccd895 chore: version 0.3.0
1381046…e93d1d0 （14 个 test: 提交，193 用例）
a1368f2 / d616e35 / bb745b7 （/mode 三提交）
9955d8d chore: vitest 测试基建
```

**验证（老板独立复核）**：`npm test` 193/193 ✅ / typecheck ✅ / build ✅ / 0.3.0 tgz ✅ / 热重载装配 ✅（fiber [active]）。

**可测面分析结论**：13 模块全部可单测（outbox/wal/state/questions/reactions/markdown-card/quota/media/send-file/doctor/permission/mode + command-router 注入点）；仅 index.ts createRuntime 闭包（ensureAgent 预设链、消息管线）无导出不可单测——等价回落链逻辑已在 commands 侧覆盖，建议未来接宿主装配级集成测试。

## 最终状态：17 项基准对齐情况

| 类别 | 项 |
| --- | --- |
| 已对齐/超越（15 项） | Outbox 零丢失、WAL 补发、表情回执、Markdown 卡片、命令三级分流、配额熔断、入站多媒体、出站文件工具、/doctor、卡片化命令（/model /permission /mode）、权限分级（三级持久化）、扫码认证（含表情权限）、意图确认卡片、会话管理、多模式 preset |
| 有意不吸（2 项） | 默认 Full access（安全取向相反）、网关锁（单实例低价值） |
| 附赠 | 正式测试 193 用例（超越参考的 156）、0.1.0 起 15 项独有能力保留 |

## 遗留

- 飞书实机端到端验证（需真实凭据；当前单测 mock 全覆盖）
- index.ts 闭包逻辑的宿主装配级集成测试（建议未来）
- 发布 0.3.0 Release（tgz 已就绪）
